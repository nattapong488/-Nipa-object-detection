import { NvisionRequest } from "../models/NvisionRequest";
import { SdkOption } from "../models/SdkOption";
import { ParameterParser, NvisionParameter } from "../utils/IParameterParser";
export declare const NvisionServiceComposer: <T extends NvisionParameter>(options: SdkOption, path: string, parameterParser: ParameterParser<T>) => {
    predict: (params: T) => Promise<NvisionRequest>;
    stream: () => {
        on: (event: string, listener: (eventArgs: any) => unknown) => void;
        once: (event: string, listener: (eventArgs: any) => unknown) => void;
        connect: () => Promise<unknown>;
        predict: (params: T) => Promise<void>;
    };
};
export default NvisionServiceComposer;
